rules_version = '2';
// Storage rules for AIra. User files under user-files/{userId}/**
service firebase.storage {
  match /b/{bucket}/o {
    match /user-files/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
