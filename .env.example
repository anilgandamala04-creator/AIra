# Firebase – sole backend (auth, Firestore, storage). Supabase is not used.
# Create a project at https://console.firebase.google.com and add a Web app to get these values.
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
# Optional: Firebase Analytics measurement ID (e.g. G-XXXXXXXXXX)
# VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id

# Optional: use Firebase emulators for local development
# VITE_USE_FIREBASE_EMULATORS=true

# AI backend URL (optional)
# - Dev: app defaults to http://localhost:5000 when running on localhost. Set VITE_API_URL only to override (e.g. different port).
# - Production: set to your deployed API URL, or leave unset for same-origin (Firebase Hosting rewrites).
# VITE_API_URL=http://localhost:5000
